# Triggers

No triggers are implemented in v0.1.0.

## Planned

- **Polling triggers** for new calendar events, service plan changes, and people updates
- **Webhook triggers** if Planning Center webhook subscriptions are configured for your organization

Webhook and polling triggers require additional design around deduplication, sample data, and subscription management. See `docs/roadmap.md` for timeline.